{
  "name": "flow2api-workspace",
  "private": true,
  "packageManager": "bun@1.3.14",
  "workspaces": [
    "apps/admin-web",
    "apps/captcha-extension",
    "apps/metadata-extension",
    "packages/api-contract",
    "packages/extension-core"
  ],
  "scripts": {
    "build": "bun run --filter '@flow2api/*' build",
    "check": "bun run --filter '@flow2api/*' check"
  }
}
